{% extends 'layouts/base.njk' %}
{% from 'macros/displayDate.njk' import date %}

{% block content %}
  <h1 class='font-size-3 title-spacing grid-container'>{{ title }}</h1>
  
  <section class="now">
    <h2 class='font-size-2 grid-container'>What I've been up to</h2>
    <div class="meta-information">{{ date('Last updated:', update) }}</div>
    <div class="details">
      {{ content | urlize | safe }}
    </div>
  </section>

  {% endblock %}
