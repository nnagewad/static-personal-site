<a class='skip-link wrapper' href='#main-content'>Skip to content</a>
<header class='wrapper flex-container--wrap vertical-align-bottom'>
  <a href='/' aria-label='{{ site.fullName }}'>
    <img src='/img/header/{{ image }}.png' alt='{{ imageAltText }}' class='site-header-image'>
  </a>
  <nav class='site-navigation' aria-label='Primary'>
    <ul class='horizontal-list'>
      {% for item in navigation.items %}
      <li>
        <a href='{{ item.url }}' {{ helpers.getLinkActiveState(item.url, page.url) | safe }}>{{ item.text }}</a>
      </li>
      {% endfor %}
    </ul>        
  </nav>
</header>
